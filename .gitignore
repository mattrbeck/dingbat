reference/

# Build outputs
src/*.out
src/*.out.dSYM/
*_debug
*_debug.dSYM/
nimcache/

# Compiled signaling server binary (build from web/signaling/server.nim)
web/signaling/signalsrv
web/signaling/signalsrv_*

# ImGui runtime state
imgui.ini

# Nim compiler cache
.cache/

# Web test deps (Playwright, installed by the render-readback test)
web/node_modules/

# WASM build artifacts and test ROMs
web/em.js
web/em.wasm
web/bios.bin
web/rom.gba
web/rom.gb
web/rom.gbc

# Native binary (root-level only)
/dingbat
/dingbat_test
/dingbat_cheat_test
/dingbat_cheat_int_test
/dingbat_test_runner
/dingbat_bench
/trade_repro
/dingbat_test_base
/netcore_trade_repro
/netcore_trade_repro_base
/rollback_trade_repro
/trade_repro_common

# Generated SRAM backing for test ROMs (created at runtime, not a fixture)
tests/roms/*.sav

# Copyrighted GBA BIOS + GB boot ROMs — LLE/boot test fixtures, NEVER commit.
# These patterns are a safety net; the real files live outside the repo
# (~/Documents/emu/...) and are referenced by absolute path when running tests.
gba_bios.bin
*bios*.bin
*boot*.bin
dmg_rom.bin
cgb_boot.bin
dmg_boot.bin
*.boot.bin

# Claude Code local state
.claude/

# Windows cross-build output
dist/

# iOS build artifacts (built by ios/build-core.sh / xcodegen)
ios/lib/
ios/Dingbat.xcodeproj/
ios/build/
ios/DerivedData/
dingbat_dist

# EXPLORATORY mp2k HLE scratch binaries
scratch_mp2kwav
scratch_bdpcm_test
scratch_probe
bench_freq
scratch_drums
scratch_mkstate
scratch_modebits_test
scratch_stateresync
scratch_bdpcm_test
tools/romfuzz/dingbat_nav
tools/romfuzz/mgba_runner
tools/romfuzz/nba_runner
__pycache__/

# never commit ROM sweep artifacts (reports may reference ROM provenance; ROMs are copyrighted)
/romfuzz-report/
/roms/
/*.gba
/*.zip
/*.sav
# Scratch harness binaries (HLE audio branch)
scratch_drums
scratch_mkstate
scratch_stateresync
scratch_modebits_test
scratch_bdpcm_test
scratch_mp2k_boot
scratch_mc
dingbat_bench_hle
