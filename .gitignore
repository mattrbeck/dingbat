reference/

# Build outputs
src/*.out
src/*.out.dSYM/
*_debug
*_debug.dSYM/
nimcache/

# Compiled signaling server binary (build from web/signaling/server.nim)
web/signaling/signalsrv
web/signaling/signalsrv_*

# ImGui runtime state
imgui.ini

# Nim compiler cache
.cache/

# Per-target nimcaches and logs from .github/scripts/build-tests.sh
.nimcache-tests/
.build-logs/

# Web test deps (Playwright, installed by the render-readback test)
web/node_modules/

# WASM build artifacts and test ROMs
web/em.js
web/em.wasm
web/bios.bin
web/rom.gba
web/rom.gb
web/rom.gbc

# Native binary (root-level only)
/dingbat
/dingbat_test
/dingbat_cheat_test
/dingbat_cheat_int_test
/dingbat_ppucomposite_test
/dingbat_ppubgunpack_test
/dingbat_ppuobjlist_test
/dingbat_savestate_compat_test
/dingbat_rewind_test
/dingbat_lcdresponse_test
/dingbat_printer_test
/rewind_codec
/rewind_codec_nopad
/dingbat_test_runner
/dingbat_bench
/dingbat_bench_native
/dingbat_ts_test
/dingbat_sdl3
/trade_repro
/dingbat_test_base
/netcore_trade_repro
/netcore_trade_repro_base
/rollback_trade_repro
/trade_repro_common

# Local investigation scratch dirs — never commit
/scratch-repro/
/.scratch/
# tools/gbscx/env.sh's ROM cache, shard/row files and witness frames
/.romcache/
/.tmp/
/.shots/

# Generated SRAM backing for test ROMs (created at runtime, not a fixture)
tests/roms/*.sav

# Copyrighted GBA BIOS + GB boot ROMs — never commit. The real files live
# outside the repo and are passed to tests by absolute path.
gba_bios.bin
*bios*.bin
*boot*.bin
dmg_rom.bin
cgb_boot.bin
dmg_boot.bin
*.boot.bin

# Claude Code local state
.claude/

# Windows cross-build output
dist/

# iOS build artifacts (built by ios/build-core.sh / xcodegen)
ios/lib/
ios/Dingbat.xcodeproj/
ios/build/
ios/DerivedData/
dingbat_dist

# Exploratory scratch harnesses (sources + compiled binaries) — never commit
scratch_*.nim
scratch_mp2kwav
scratch_probe
bench_freq
scratch_mp2k_boot
scratch_mc
scratch_drums
scratch_mkstate
scratch_modebits_test
scratch_stateresync
dingbat_bench_hle
# compiled emulator binary emitted next to src/dingbat/gba/gba.nim
/src/dingbat/gba/gba
tools/romfuzz/dingbat_nav
tools/romfuzz/mgba_runner
tools/romfuzz/nba_runner
# built by tools/gbdiff/build.sh against a docboy checkout outside this repo
tools/gbdiff/docboy_gb_runner_dmg
tools/gbdiff/docboy_gb_runner_cgb
__pycache__/

# never commit ROM sweep artifacts (reports may reference ROM provenance; ROMs are copyrighted)
/romfuzz-report/
/roms/
/*.gba
/*.gb
/*.gbc
/*.zip
/*.sav
# save exports, hardware photos, stray scp/debug artefacts at the root
/*.sps
/*.xps
/*.gsv
/*.jpg
/*.jpeg
/*.png
/root@*
/.vtmp/
/hwphotos/
web/signaling/signalsrv-*

# Runtime save written when the web demo ROM is played locally
web/*.sav

# Browser perf bench: ROMs/states are supplied locally (see web/bench/README.md)
web/bench/*.gba
web/bench/*.gb
web/bench/*.gbc
web/bench/*.state
web/bench/*.sav
dingbat_bench_d
dingbat_bench_orig

# Per-intdefine sweep binaries (tools/gbppu/cgbsweep.sh)
/dt_*
/dtr_*
web/*.gba
/dingbat_sgb_test
/dingbat_printer_test
/dingbat_ts_test
/dingbat_savestate_compat_test
/dingbat_rewind_test
web/_serve8781.py

# Hostile-input save-state fuzzer (`nimble statefuzz_build`)
/statefuzz
/statefuzz_dbg
flashcart-kit/
.DS_Store

# Personal ROM list consumed by tools/gbgate/sweep.sh (supplied locally)
tools/gbgate/roms.txt
/dingbat_clipreplay_test
